{
    "name": "@aurora/prisma",
    "version": "0.1.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "generate": "doppler run npx prisma generate && tsc && copy src/index.d.ts dist/index.d.ts",
        "reset": "doppler run npx prisma db seed",
        "studio": "doppler run npx prisma studio"
    },
    "prisma": {
        "seed": "ts-node src/seed.ts"
    },
    "devDependencies": {
        "@types/node": "^18.11.18",
        "prisma": "^4.8.0",
        "typescript": "^4.9.4"
    },
    "bundledDependencies": [
        "@prisma/client",
        ".prisma"
    ],
    "dependencies": {
        "@prisma/client": "^4.8.0"
    }
}
