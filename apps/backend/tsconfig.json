{
    "extends": "../../tsconfig.build.json",

    "compilerOptions": {
        "baseUrl": "./src",
        "paths": {
            "@backend/*": ["*"]
        },
        "outDir": "build",
        "strictNullChecks": true
    },
    "ts-node": {
        "require": ["tsconfig-paths/register"]
    }
}
